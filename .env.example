# SQLite file used by Prisma. Path is relative to the prisma/ directory.
DATABASE_URL="file:./dev.db"

# Optional: when set, prompts that the rule engine cannot parse are sent to OpenAI.
# The local demo works without this key.
# OPENAI_API_KEY=
# OPENAI_MODEL=gpt-4o-mini
